[project]
name = "ccmto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "hhgzeng", email = "hanhanguaizj@gmail.com" }]
requires-python = ">=3.13"
dependencies = [
    "cec2013lsgo>=2.2",
    "numpy>=2.5.1",
    "setuptools==70.3.0",
]

[project.scripts]
ccmto = "ccmto:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv.extra-build-dependencies]
cec2013lsgo = ["Cython"]
